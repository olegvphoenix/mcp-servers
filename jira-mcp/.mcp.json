{
  "mcpServers": {
    "jira": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/olegvphoenix/mcp-servers.git#subdirectory=jira-mcp",
        "jira-mcp"
      ],
      "env": {
        "JIRA_URL": "${JIRA_URL}",
        "JIRA_USERNAME": "${JIRA_USERNAME}",
        "JIRA_PASSWORD": "${JIRA_PASSWORD}",
        "MCP_USERNAME": "${MCP_USERNAME}",
        "MCP_PASSWORD": "${MCP_PASSWORD}",
        "JIRA_DEFAULT_PROJECT": "${JIRA_DEFAULT_PROJECT}",
        "JIRA_DEFAULT_ISSUE_TYPE": "${JIRA_DEFAULT_ISSUE_TYPE}",
        "JIRA_DEFAULT_CUSTOM_FIELDS": "${JIRA_DEFAULT_CUSTOM_FIELDS}"
      }
    }
  }
}
